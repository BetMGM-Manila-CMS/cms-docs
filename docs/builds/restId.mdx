---
last_update:
  date: 1/25/2023
  author: Kyle Atienza
  description: Add section creating and commenting
authors: Kyle Atienza
title: Rest ID
---

# Rest ID

## Creating a Rest ID

To create a Rest ID go to the Sitecore path mentioned below and duplicate an existing Rest ID Item.

<Admonition type="info" title="Sitecore Path">
  Content Editor > Marketing > US > CMS-NEW-FOLDER > state-brand > product > year > month
</Admonition>

Name the new created Rest ID same with the name of your ticket. Just remove unwanted characters.

<Admonition type="tip" title="Tip">
  Vivian from US provided a template folder inside the the [US Sitecore NJ Folder](http://cms-us.prod.env.works/sitecore/DirectLink.aspx?fo={64853FDD-BE01-41A1-AFBD-AC7536EC6773}\&la=), we can just `copy to` these items (except to ON) to the correct folder.

  ![](/img/builds/restId/restid-template.png)
</Admonition>

### Grouped Rest IDs

Rest IDs needs to be grouped if a ticket is requiring more than one Rest ID for that create a folder and name it the name of your ticket. Then place the different Rest IDs inside the folder. See examples below.

<Tabs>
  <TabItem value="tiered" label="Tiered">
    ![](/img/builds/restId/restid-tiered.png)
  </TabItem>

  <TabItem value="engagement" label="Engagement">
    ![](/img/builds/restId/restid-engagement.png)
  </TabItem>
</Tabs>

## SectionSelector

Rest ID has different sections that can be displayed or hidden, it will be adjusted depending on the needs of your ticket.

![](/img/builds/restId/restid-section-select.png)

<Admonition type="warning" title="note">
  When duplicating Rest ID components most fields will be populated with contents even though it is blank from the source item.
</Admonition>

## Promohub Section

<Admonition type="note" title="note">
  Get the template value for this section either on your monday.com ticket mentioned by your sub-lead or in the Project Details of your Workfront ticket under the `CRM Ops` section.

  ![](/img/builds/restId/restid-wf-template.png)
</Admonition>

### Required Fields

The team uses an [internal tool](http://betmgm-phtools.entaingroup.corp/promohub-templates/) to get the appropriate value for the fields.

![](/img/builds/restId/restid-rewards-tool.png)

To use the tool, click through each icon to get to the desired values that will be displayed on the right side of the tool and paste the appropriate values on the following fields.

<TinaTable>
  <TinaTableRow items={[{ label: "Fields", header: true }]} />

  <TinaTableRow items={[{ label: "CommonTitle", style: "highlight" }]} />

  <TinaTableRow items={[{ label: "CommonDescription", style: "highlight" }]} />

  <TinaTableRow items={[{ label: "CommonTermsAndConditions", style: "highlight" }]} />
</TinaTable>

### Required Images

The fields `Image Landscape` and `Image Square` will be placed with the same image. Depending on the type of the ticket we will either use `Promohub Tiles-800x360` or `Promohub Reward Tiles-800x360`. Please see guide below:

<TinaTable>
  <TinaTableRow
    items={[
      { label: "Provided PAT/EDS", header: true },
      { label: "Image", header: true }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "Yes" },
      { label: "Promohub Tiles-800x360", style: "highlight" }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "No" },
      { label: "Promohub Reward Tiles-800x360", style: "highlight" }
    ]}
  />
</TinaTable>

## Inbox

The Inbox is a section for the Rest ID component that holds the values for the notification messages whenever a user recieves one.

Here is an example from BetMGM:

![](/img/builds/restId/restid-betmgm-inbox.png)

### Required Fields

<TinaTable>
  <TinaTableRow
    items={[
      { label: "Field", header: true },
      { label: "Value", header: true }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "PreviewTitle" },
      { label: "Player Inbox > Preview Title", style: "highlight" }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "PreviewDescription" },
      { label: "Player Inbox > Preview Description", style: "highlight" }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "FullViewTitle" },
      { label: "Player Inbox > Fullview Title", style: "highlight" }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "FullViewDescription" },
      { label: "Player Inbox > Fullview Description", style: "highlight" }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "ManualTermsAndConditions" },
      { label: "See Terms Doc", url: "./documents#terms-and-conditions" }
    ]}
  />
</TinaTable>

<Admonition type="info" title="info">
  Plese check the [CTA guide](../builds/cta) for the proper labeling and link to use.
</Admonition>

### Required Images

<TinaTable>
  <TinaTableRow
    items={[
      { label: "Field", header: true },
      { label: "Image", header: true }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "PreviewImage" },
      { label: "Inbox_short_50x50", style: "highlight" }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "FullViewImage" },
      { label: "Inbox-Detail-690x246", style: "highlight" }
    ]}
  />
</TinaTable>

## Overlay

The Overlay is a section for the Rest ID component that holds the values for a popup message.

Here is an example:

![](/img/builds/restId/restid-overlay-eg.png)

Here are the required fields for the overlay component

1. **Overlay Title** - add the overlay title
2. **Overlay Description** - add the overlay  description
3. **Overlay CTA** - add the overlay CTA. For more info refer to the CTA page.
4. **Overlay Image** - use image Overlay EDS-840x246.
5. **CTANativeLink URL** - use the URL you used for the overlay CTA
6. **CTANativeLink Link Text** - use the text you used for the overlay CTA

<Admonition type="info" title="CTA Casing">
  * Use camel casing for BetMGM
  * Use capitalized for other labels
</Admonition>

<Admonition type="info" title="CTA Styling">
  Do not add any inline style or class tot the CTA.
</Admonition>

### Old Format

<Admonition type="note" title="Note">
  The old format for the overlay component is still used for Sports ticket, but it may change in the future as they may also adapt the new format.
</Admonition>

##### Required Fields

<TinaTable>
  <TinaTableRow
    items={[
      { label: "Field", header: true },
      { label: "Value", header: true }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "PreviewTitle" },
      { label: "Overlay > Overlay Description", style: "highlight" }
    ]}
  />
</TinaTable>

<Admonition type="note" title="note">
  If the copy mentions a title for the Overlay put it inside `<h2></h2>` and place on the top before the description.
</Admonition>

<Admonition type="info" title="info">
  Plese check the [CTA guide](../builds/cta) for the proper labeling and link to use.
</Admonition>

##### Required Images

<TinaTable>
  <TinaTableRow
    items={[
      { label: "Field", header: true },
      { label: "Image", header: true }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "PreviewTitle" },
      { label: "Overlay EDS-840x246", style: "highlight" }
    ]}
  />
</TinaTable>

## Toaster

The Toaster is a section for the Rest ID component that holds the values for the toaster components that attracts the user's attention for notifications and messages.

Here is an example:

![](/img/builds/restId/restid-toaster-eg.png)

Here are the required fields for the toaster component:

1. Check the `Close With Timer` checkbox
2. **Toaster Title** - add the toaster title
3. **Toaster Description** - add the toaster description
4. **Toaster CTA** - add the toaster CTA. For more info refer to the CTA page.

<Admonition type="info" title="CTA Casing">
  * Use camel casing for BetMGM
  * Use capitalized for other labels
</Admonition>

<Admonition type="note" title="CTA Styling">
  Do not add any inline style or class tot the CTA.
</Admonition>

### Old Format

#### Required Fields

<TinaTable>
  <TinaTableRow
    items={[
      { label: "Field", header: true },
      { label: "Value", header: true }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "PreviewTitle" },
      { label: "Toaster", style: "highlight" }
    ]}
  />
</TinaTable>

<Admonition type="info" title="note">
  Enclose the toaster in `<a>` tag and add the CTA link to its `href` attribute
</Admonition>

<Admonition type="info" title="info">
  Plese check the [CTA guide](../builds/cta) for the proper labeling and link to use.
</Admonition>

#### Required Images

<TinaTable>
  <TinaTableRow
    items={[
      { label: "Field", header: true },
      { label: "Image", header: true }
    ]}
  />

  <TinaTableRow
    items={[
      { label: "PreviewTitle" },
      { label: "Toaster-65x65", style: "highlight" }
    ]}
  />
</TinaTable>

## Commenting

Right click your Rest ID and copy both the Rest ID and the Direct Link, provide both these for your ticket comment.

![](/img/builds/restId/restid-comment-value.png)

<Admonition type="tip" title="tip">
  Embed the direct link of the Rest ID on the Rest ID itself or on a 'DL' beside it.

  You can also provide a short description for what the Rest ID is(`IOT`, `CB-Fixed No Deposit Wager`, `BS-Fixed Deposit Wager`, etc...).

  ![](/img/builds/restId/restid-comment.png)
</Admonition>
